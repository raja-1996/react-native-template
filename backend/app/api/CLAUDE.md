# api/
Namespace package that organises all versioned API route modules; contains only `v1/`.

- `__init__.py` — empty package marker; no exports
- `v1/` — versioned route handlers for auth, todos, and file storage
